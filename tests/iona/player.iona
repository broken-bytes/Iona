module App

use UnityEngine

public class Player: MonoBehaviour {
    public var health = 100

    fn onCollisionEnter(collision: Collision) {
        var name = collision.gameObject.name

        self.gameObject.addComponent<DamageTicker>()
    }
}